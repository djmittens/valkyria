(load "src/prelude.valk")
(load "src/modules/test.valk")

(def {aio} (aio/await (aio/start)))

(printf "Testing map with 9 elements\n")
(def {nums} (list 1 2 3 4 5 6 7 8 9))
(def {result} (map (\ {x} {
  (printf "Processing %d\n" x)
  (* x 2)
}) nums))
(printf "Map result: %s\n" (str result))

(aio/stop aio)
