(load "src/prelude.valk")

(print "Testing macro expansion")

(def {test-macro} (macro {body} {
  (print "Macro body:" body)
  (def {result} (list print "Expanded:" body))
  (print "Returning:" result)
  result
}))

(print "Calling macro")
(test-macro "Hello")
(print "Done")

(shutdown 0)
