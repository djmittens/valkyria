; Test HTTP/2 error handling - connection to unreachable host
; This test is expected to FAIL with a connection error

(do
  (print "Attempting connection to unreachable port...")

  ; Try connecting to localhost on a port that should be closed
  (= {conn-future} (http2/connect aio "localhost" 9999))
  (= {conn-result} (await conn-future))

  ; If we reach here, connection unexpectedly succeeded
  (error "Expected connection to fail but it succeeded")
)
