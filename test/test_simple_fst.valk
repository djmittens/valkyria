(load "src/prelude.valk")

(printf "Simple fst test:\n")
(= {lst} {10 20 30})
(printf "List: ")
(print lst)

(printf "\nCalling fst:\n")
(= {result} (fst lst))
(printf "Result: ")
(print result)

(printf "\nDone\n")
(shutdown 0)
