(load "src/prelude.valk")

(print "Test recursion")
(fun {countdown n} {
  do
    (printf "n = %ld\n" n)
    (if (<= n 0)
      {0}
      {countdown (- n 1)})
})

(print "Calling countdown 3")
(def {result} (countdown 3))
(print "Result:")
(print result)
