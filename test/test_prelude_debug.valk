; Debug version of prelude test
(load "src/prelude.valk")
(load "src/modules/test.valk")

; Simple test
(test/define "simple-test"
  {do
    (print "Running simple test")
    (test/assert-eq 5 (+ 2 3) "2+3 should equal 5")
    (print "Test passed")
    true
  })

(print "About to run tests")
(test/run {})
(print "After test run")
