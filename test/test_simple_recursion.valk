(load "src/prelude.valk")

(print "Test simple recursion without do")
(fun {count n} {
  if (<= n 0)
    {0}
    {count (- n 1)}
})

(print "Calling count 3")
(def {result} (count 3))
(print "Result:")
(print result)
