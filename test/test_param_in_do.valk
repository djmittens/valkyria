(load "src/prelude.valk")

(printf "Test 1: nth with variables\n")
(= {idx} 1)
(= {list} {100 200 300})
(printf "Result: %ld\n" (nth idx list))

(printf "\nTest 2: Inline nth call in do\n")
(do
  (= {idx2} 1)
  (= {list2} {100 200 300})
  (printf "Result: %ld\n" (nth idx2 list2)))

(shutdown 0)
