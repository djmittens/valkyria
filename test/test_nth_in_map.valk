(load "src/prelude.valk")

(fun {get-first item} {
  (nth 1 item)
})

(= {data} {{100 200} {300 400}})
(printf "Testing map with nth:\n")
(= {result} (map get-first data))
(printf "Result: ")
(print result)

(shutdown 0)
