; Test continuation capture
(print "Testing continuation capture...")

(def {result}
  (async-reset
    (+ 10 (async-shift {k}
      (do
        (print "Captured continuation k")
        (print "Type of k:" (type k))
        ; Return 5 without calling continuation
        5)))))

(print "Result:" result)
(print "Done")