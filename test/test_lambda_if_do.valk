(print "Test 1: lambda with if and do - direct lambda syntax")
(def {test1} (\ {x} {
  if (== x 0)
    {do (print "Zero") 999}
    {do (print "Not zero") (+ x 100)}
}))

(print "Calling test1 0")
(print (test1 0))
(print "Calling test1 5")
(print (test1 5))
