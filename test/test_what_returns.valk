(load "src/prelude.valk")

(printf "Test head return type:\n")
(= {mylist} {100 200 300})
(= {result} (head mylist))
(printf "Type: %ld\n" (type result))
(printf "Length: %ld\n" (len result))

; Print the result itself
(print result)

(shutdown 0)
