(load "src/prelude.valk")

(printf "Test without eval:\n")
(= {mylist} {100 200 300})
(= {first} (head mylist))
(printf "First (without eval): %ld\n" first)

(printf "\nTest with eval:\n")
(= {first2} (eval (head mylist)))
(printf "First (with eval): %ld\n" first2)

(shutdown 0)
