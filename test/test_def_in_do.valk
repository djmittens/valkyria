(load "src/prelude.valk")

(printf "Test tail:\n")
(= {mylist} {100 200 300})
(= {rest} (tail mylist))
(printf "Tail type: %ld, len: %ld\n" (type rest) (len rest))
(print rest)

(printf "\nTest fst on rest:\n")
(= {second} (fst rest))
(printf "Second: %ld\n" second)

(shutdown 0)
