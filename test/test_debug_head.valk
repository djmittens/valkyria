(printf "Debug head issue:\n")

; Test head outside function first
(= {lst} {10 20 30})
(printf "List created\n")

(printf "Calling head outside function: ")
(= {h1} (head lst))
(print h1)

(printf "\nDone\n")
(shutdown 0)
