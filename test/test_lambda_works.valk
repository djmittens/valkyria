; Test if the lambda actually works even though it prints as (\ )
(def {f} {test x})
(def {b} {(+ x 10)})

(print "Creating lambda with (\ (tail f) b):")
(def {mylambda} (\ (tail f) b))
(print "Lambda:")
(print mylambda)

(print "Calling mylambda with 32:")
(def {result} (mylambda 32))
(print "Result:")
(print result)
