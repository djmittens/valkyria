; Simple test runner to debug the issue
(load "src/prelude.valk")
(load "src/modules/test.valk")

; Reset the test registry
(def {*test-registry*} {})

; Define just a few tests to isolate the issue
(test/define "test-1" {true})
(test/define "test-2" {true})
(test/define "test-3" {true})

(printf "About to run tests...\n")
(test/run)
