(load "src/prelude.valk")

(fun {test-eval n} {
  do
    (printf "n in function: %ld\n" n)
    (printf "Evaling {(+ n 1)}:\n")
    (= {result} (eval {(+ n 1)}))
    (printf "Result: %ld\n" result)
})

(printf "Calling test-eval 42:\n")
(test-eval 42)

(shutdown 0)
