; Basic tests for shift/reset continuations

(print "Testing shift/reset continuations")

; Test 1: Basic reset (should just evaluate)
(print "Test 1: Basic reset")
(def {result1} (reset (+ 1 2)))
(print result1)  ; Should print 3

; Test 2: shift returns error (VM integration not complete)
(print "Test 2: Shift (expect error for now)")
(def {result2} (reset (shift k 10)))
(print result2)  ; Will show error about VM integration

(print "Tests complete")