(load "src/prelude.valk")

(= {cs} {{10 20}})

(printf "Test: (print (fst cs))\n")
(print (fst cs))

(printf "\nTest: via if\n")
(= {r} (if 1 {do
  (printf "In do\n")
  (print (fst cs))
  42
} {99}))
(printf "r = ")
(print r)
