(load "src/prelude.valk")

(printf "Test 1: curry with +\n")
(= {add_curry} (curry +))
(printf "Calling with {1 2 3}:\n")
(= {result} (add_curry {1 2 3}))
(printf "Result: %ld\n" result)

(shutdown 0)
