; Test def inside lambda body with OLD explicit sexpr syntax
(def {test-lambda} (\ {x} {(def {result} (+ x 10))}))

(print "Calling test-lambda 5:")
(test-lambda 5)

(print "Checking if result was defined:")
(print result)
