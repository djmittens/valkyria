(load "stdlib/prelude.valk")
(load "stdlib/test/test.valk")
(def {aio} (aio/await (aio/start)))
(def {result} (shutdown 0))
(test/run (list
  (test/case "shutdown-returns-code" {(== result 0)})
) {:suite-name "Shutdown"})
