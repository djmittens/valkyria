; Test async-shift
(print "Testing async-shift...")

(async-reset
  (do
    (print "Before shift")
    (async-shift {k}
      (do
        (print "Inside shift")
        (print "Continuation k =" k)
        99))
    (print "After shift (should not print)")))

(print "Done")