(load "src/prelude.valk")

(print "Test if branches")
(fun {test-if x} {
  if (== x 0)
    {do (print "Branch A") 999}
    {do (print "Branch B") (+ x 100)}
})

(print "Calling test-if 0")
(print (test-if 0))
(print "Calling test-if 5")
(print (test-if 5))
