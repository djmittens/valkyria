(load "src/prelude.valk")
(load "src/modules/test.valk")

(def {aio} (aio/start))

(printf "Testing map with aio/pure\n")
(def {nums} (list 1 2 3 4 5 6 7 8 9))
(def {result} (map (\ {x} {
  (printf "Processing %d\n" x)
  (aio/pure x)
}) nums))
(printf "Map done\n")

(aio/stop aio)
